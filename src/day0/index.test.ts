import { getPuzzle1Answer, getPuzzle2Answer } from '.'

describe('getPuzzle1Answer', () => {
  it('returns an answer', () => {
    expect(getPuzzle1Answer()).toMatchInlineSnapshot(`0`)
  })
})

describe('getPuzzle2Answer', () => {
  it('returns an answer', () => {
    expect(getPuzzle2Answer()).toMatchInlineSnapshot(`0`)
  })
})
